---- Streams -----

1: Streams is useful to handle large sized file for reading because we can't use fs module's readfile method to read large file.

2: Strems have listeners that will recieve the chunk of data, not the entire file at same time.

