1 : Node Js is not a programming language. It is just a runtime environment to run JS outside the browser.

2: Since, JS only runs in Browser, Node makes the chance to run JS outside the browser.

3: Node Js is famous for its asynchronous nature of handling multiple request.

4: And Js is well known for I/O based operations not for CPU intensive tasks.

5: But with worker module we can split the cpu intensive task to multiple threads to handle multipe cpu intensive request.

---- Modules----

1: In the initial javascript, the fucntions and variables of the one file can share to another file without explictly importing and exporting it.

2: It will leads to conflict.

3: That's when the es6 JS comes in with import and export objects which is used to do those things and it will not allow to share without these things.

